You have completed the initial native SwiftUI generation of the Empower Humanity AI iOS application.

The initial implementation is successful.

DO NOT regenerate the application.
DO NOT convert it to React Native, Expo, Flutter, Capacitor, or any JavaScript mobile framework.
DO NOT redesign successful screens.

Perform one focused native iOS/Xcode refinement pass before we download the repository and test it locally in Xcode.

We understand Replit cannot compile the iOS project because it does not provide a macOS/Xcode environment. That is expected. Your job is to improve source/project consistency and provide the cleanest possible downloadable Xcode project.

The existing SwiftUI product experience, design system, feature screens, repositories, mock data, side-menu navigation, iPad adaptation, and Ben/Alex demo should all be preserved.

# 1. Correct Access-State Routing

The current AppState supports:

- unauthenticated
- authenticated
- AccessTier

but AppRouter currently sends every authenticated user into AuthenticatedRootView.

Correct this.

Canonical routing must be:

Unauthenticated
→ PublicNavigationRoot / Landing

Authenticated + Public
→ Access Status

Authenticated + Waitlist
→ Access Status

Authenticated + Approved Beta
→ AuthenticatedRootView

Authenticated + Founder Access
→ AuthenticatedRootView

Authenticated + Design Partner
→ AuthenticatedRootView

Authenticated + Subscriber
→ AuthenticatedRootView

Do not scatter access checks through feature Views.

Centralize this decision in AppRouter/AppState/AccessService.

The architecture should later support feature-level entitlements as well.

# 2. Preserve Signup Behavior

A newly created mock account should still default to:

AccessTier.waitlist

After signup:

Signup
→ authenticated account
→ Access Status

It must NOT enter Today simply because authState is authenticated.

# 3. Refine Debug Demo Behavior

The current Debug configuration automatically starts authenticated as Ben.

Change this.

Both Debug and Release should normally start:

Unauthenticated
→ Landing

Debug should additionally expose a clearly marked:

Explore Demo

or:

Continue with Ben & Alex Demo

action.

Only Debug builds may expose this demo action.

Use:

#if DEBUG

or a clear AppConfiguration flag.

Release must have:

- no automatic Ben session,
- no visible demo-login CTA,
- no hard-coded production credentials.

Preserve Ben/Alex mock data and DemoWorkflowCoordinator for development.

# 4. Keep the Public Experience Testable

Because Debug will no longer auto-login, ensure a local developer can test:

Landing
→ Login
→ Signup
→ Access Status
→ Demo Login
→ Today

without modifying source code.

# 5. Preserve Side Menu as Primary Authenticated Navigation

The current iPhone implementation correctly uses:

- leading menu button,
- overlay drawer,
- dimmed backdrop,
- animated leading panel.

The current iPad implementation correctly uses NavigationSplitView.

Preserve this architecture.

Do NOT replace it with a TabView or bottom-tab global navigation.

Primary destinations remain:

Today
Chat
Work
Goals
Knowledge
Activity

Additional:
AI Work
Approvals
Devices
Notifications

System:
Integrations
Settings

# 6. Refine Navigation Architecture for Detail Routes

Top-level drawer destination selection may remain enum-driven.

However, prepare the authenticated experience for real NavigationStack detail navigation.

Support future route concepts such as:

chat/{conversationId}
goal/{goalId}
task/{taskId}
project/{projectId}
ai-work/{workId}
approval/{approvalId}
device/{deviceId}
artifact/{artifactId}

Use NavigationPath / Hashable navigation destinations where appropriate.

Do not build every missing detail feature.

The goal is to ensure that:

notification
→ approval detail

notification
→ AI Work detail

activity
→ relevant artifact/work item

can be implemented cleanly later.

# 7. NotificationRouter Alignment

Review NotificationRouter against the navigation architecture.

Prepare it to produce typed navigation destinations rather than fragile string routes where possible.

Do not configure real APNs yet.

# 8. Clean the Repository for Native iOS Handoff

The actual product is located in:

EmpowerHumanityAI/
    EmpowerHumanityAI.xcodeproj
    EmpowerHumanityAI/
    README.md

The repository root currently contains Replit/Node tooling such as:

.npmrc
package.json
pnpm-lock.yaml
pnpm-workspace.yaml
scripts/
lib/
and other Replit-generation artifacts.

Review each of these.

If a file exists ONLY to help Replit generate/validate the project and is not needed after generation, remove it from the final native iOS repository or clearly isolate it outside the downloadable Xcode application.

The final repository should not imply that Node, npm, pnpm, React, or JavaScript is required to build or run the iOS application.

The native application must remain completely buildable from Xcode without Node tooling.

Keep `.replit` files only if Replit still requires them during this refinement pass. They must not be referenced by the Xcode project.

# 9. Do Not Add React Native

Explicitly verify there is no:

React Native
Expo
Metro
JavaScript UI
TypeScript mobile UI
Capacitor
Ionic
Flutter

inside the actual iOS application.

Do not add any of them.

# 10. Xcode Project Integrity Audit

Carefully audit:

EmpowerHumanityAI.xcodeproj/project.pbxproj

Because we will download this project and open it in Xcode locally.

Verify:

1. exactly one primary application target exists unless another target is genuinely required;
2. every PBXFileReference points to a real file;
3. every PBXBuildFile refers to a valid PBXFileReference;
4. every file in Sources actually exists;
5. every resource in Resources exists;
6. Assets.xcassets exists;
7. Info.plist exists;
8. no duplicate source references exist;
9. no duplicate source build-phase entries exist;
10. no absolute Replit filesystem paths exist;
11. no missing source files are referenced;
12. no orphan file IDs exist where practical to detect;
13. no external package references are needed;
14. target/product naming is consistent;
15. Debug and Release configurations exist.

Do NOT claim Xcode compilation succeeded.

You cannot run Xcode here.

Report only static consistency validation.

# 11. Preserve Bundle Identity

Keep:

Product:
Empower Humanity AI

Target:
EmpowerHumanityAI

Bundle identifier:
ai.empowerhumanity.EmpowerHumanityAI

Deployment:
iOS 17+

Device family:
iPhone + iPad

Do not introduce Replit-generated bundle IDs.

# 12. Correct Info.plist Hardware Requirements

Review Info.plist.

Remove the legacy:

UIRequiredDeviceCapabilities → armv7

requirement.

This is an iOS 17+ SwiftUI application targeting modern iPhone/iPad hardware.

Do not introduce unnecessary hardware capability restrictions.

Preserve appropriate usage descriptions for:

Face ID
Microphone
Speech Recognition

if the corresponding architecture remains present.

# 13. Keep Capabilities Conservative

Do not add unnecessary entitlements merely because future functionality may need them.

Do not add broken:

Push Notifications entitlement
Background Modes
Associated Domains
Sign in with Apple
Keychain Sharing

unless fully configured.

Architecture interfaces/stubs are sufficient for now.

We will configure Apple capabilities locally when implementing the real integrations.

# 14. Correct Client-Side Security Documentation

The README currently references adding a SESSION_SECRET to Config.xcconfig.

REMOVE this instruction.

A server-side session secret must never be embedded in an iOS client.

The iOS app may eventually contain public/non-secret configuration such as:

API_BASE_URL

The app may securely store user-specific:

access token
refresh token

in Keychain after authentication.

Do NOT place:

server secrets
private signing secrets
database credentials
AI provider keys

inside the application bundle or xcconfig committed to the project.

Update the README accordingly.

# 15. API Architecture

Preserve APIClient / URLSession architecture.

Canonical future communication remains:

iOS App
↓
Empower Humanity NestJS Core API
↓
Empower Humanity AI Services
↓
AI Native Substrate
↓
AI Native Runtime when execution is needed

Do not add direct:

OpenAI
Anthropic
Gemini

SDK integrations for normal product interaction.

# 16. Repository Architecture

Preserve the existing protocol-first repository direction.

The current documented architecture includes repositories for product domains and mock implementations.

Do not consolidate them into one massive singleton.

Ensure mock repositories can later be swapped for API-backed implementations.

# 17. Demo Workflow

Preserve DemoWorkflowCoordinator.

The flagship simulated flow should remain possible:

Ben asks Alex to review deployment logs
→ Chat updates
→ AI Work created
→ Home Mac Mini shown
→ Approval required
→ Ben approves
→ AI Work continues
→ Activity updated
→ Notification/result generated
→ Alex reports result

Keep this clearly development/mock functionality.

# 18. Biometric Approval Accuracy

Preserve BiometricApprovalService and LocalAuthentication architecture.

Do not imply that every action requires biometrics.

Correct conceptual language:

Sensitive actions may require explicit approval.
Biometric authentication can be used to confirm the human approving an action when policy requires it.

Do not falsely claim successful Face ID/Touch ID verification if only mock behavior is occurring.

# 19. Human-Facing Terminology Audit

Ensure the app defaults to human-facing terminology.

Prefer:

Alex
Your co-intelligence
Today
Active Work
Approvals
Devices
Dedicated AI Compute
Knowledge
Activity
Permissions
Autonomy

Avoid exposing by default:

Runtime Node
Execution Fabric
Inference Router
Capability Provider
Substrate
Worker
MCP Server

Infrastructure terminology may appear only under future Advanced Details.

# 20. AppState Responsibility

Keep AppState focused primarily on truly global concerns:

authentication
access
current user
primary co-intelligence identity
global AI pause state
global routing state where needed

Do not make AppState become a repository for every product domain.

Feature data should remain in repositories/ViewModels.

# 21. Public Navigation

Preserve NavigationStack for the public experience.

Ensure:

Landing → Login
Landing → Signup
Login → Signup
Signup → Login

and correct Back behavior.

After signup:

→ Access Status

After authorized sign-in:

→ Today

# 22. iPad Adaptation

Preserve the current design:

Compact width:
overlay side drawer

Regular width:
NavigationSplitView / persistent sidebar

Do not duplicate completely separate iPhone/iPad feature implementations.

# 23. Design System Preservation

Preserve:

EHColors
EHTypography
EHSpacing
EHRadius
EHShadow
EHGradients
CoIntelligenceOrb
EHComponents

Preserve the six-color Intelligence Spectrum and bright/light canonical design.

Do not replace the product identity with stock iOS blue or generic SwiftUI styling.

# 24. Do Not Add Third-Party Dependencies

The current Xcode target has no Swift Package dependencies.

Keep it that way for this handoff pass unless absolutely necessary.

Prefer Apple-native frameworks:

SwiftUI
Foundation
Observation
LocalAuthentication
UserNotifications
AVFoundation
Speech
Security

No CocoaPods.
No Carthage.
No unnecessary Swift Packages.

# 25. Assets Audit

Verify:

Assets.xcassets
AppIcon.appiconset
AccentColor.colorset

are structurally valid.

Placeholder icon artwork is acceptable.

Do not create file references to missing image assets.

# 26. README Handoff Update

Update README to clearly state:

This project was generated in Replit without access to Xcode.

The source/project structure has been statically reviewed for consistency.

The project has NOT been compiled in Xcode.

Local validation steps:

1. Clone/download repository.
2. Open EmpowerHumanityAI/EmpowerHumanityAI.xcodeproj.
3. Allow Xcode to update recommended project settings only after review.
4. Select development team if signing to a physical device.
5. Select iPhone simulator.
6. Build.
7. Fix any Xcode-specific project metadata/compiler issues encountered.
8. Test iPad layout separately.

Also document:

Bundle ID
iOS deployment target
Debug demo behavior
Release behavior
Mock vs real integrations

# 27. Do Not Pretend to Build

Your final completion report must explicitly say:

Native SwiftUI source generated.
Xcode project statically validated.
Xcode compilation has not been performed because Replit does not provide Xcode/macOS tooling.

Do not report "build passed."

# 28. Final Static Handoff Audit

Before finishing, verify that the downloadable project contains:

EmpowerHumanityAI/
├── EmpowerHumanityAI.xcodeproj/
│   └── project.pbxproj
├── EmpowerHumanityAI/
│   ├── App/
│   ├── Core/
│   ├── Data/
│   ├── DesignSystem/
│   ├── Features/
│   ├── Assets.xcassets/
│   └── Info.plist
└── README.md

Confirm that all Swift files used by the app are referenced by the app target.

# 29. Completion Report

Provide:

1. Files changed.
2. Access-routing correction.
3. Debug/release behavior.
4. Navigation changes.
5. Repository/tooling cleanup.
6. Info.plist correction.
7. README security correction.
8. Xcode project consistency findings.
9. Anything that still must be verified locally in Xcode.

# 30. Final Requirement

Do not expand product scope.

Do not generate additional unrelated features.

Do not convert frameworks.

The goal of this pass is:

Generated SwiftUI prototype
↓
Clean native iOS project
↓
Download
↓
Open in Xcode
↓
Local compile/test
↓
Enter GRD development

Produce the cleanest possible native SwiftUI Xcode handoff.